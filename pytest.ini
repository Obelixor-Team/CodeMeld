[pytest]
pythonpath = .
markers =
    unit: Unit tests that mock or isolate functionality
    integration: Tests full workflows
    regression: Edge or bug-specific tests
addopts = --cov=src --cov-report=term-missing
