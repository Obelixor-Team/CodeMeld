# Code Combiner

A simple Python script to scan a directory and combine all code files into a single text file.

## Usage

```bash
python code_combiner.py <directory> [options]
```

### Options

- `-o, --output`: Specify the output file name (default: `combined_code.txt`).
- `-e, --extensions`: Specify custom file extensions to include (space-separated).
